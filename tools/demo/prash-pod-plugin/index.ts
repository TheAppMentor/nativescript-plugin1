import { DemoSharedBase } from '../utils';
import {  } from '@prash/prash-pod-plugin';

export class DemoSharedPrashPodPlugin extends DemoSharedBase {

  testIt() {
    console.log('test prash-pod-plugin!');
  }
}